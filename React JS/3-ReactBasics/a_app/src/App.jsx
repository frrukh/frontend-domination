import React from "react";  // we should import React as the jsx will converted into React.createElement.

function App(){
  // return wants to write the returning thing in same line, to extend it to next line we can wrap the returning element in ().
  return (
    <h1>hi this is h1</h1>
  )
}

// we have to export the component to get this in main file.
export default App;